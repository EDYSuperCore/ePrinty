# 快速开始指南

## 三分钟快速启动

### 1️⃣ 安装依赖（首次运行）

```bash
# 确保已安装 Node.js 和 Rust
npm install
```

### 2️⃣ 启动开发模式

```bash
npm run tauri dev
```

### 3️⃣ 打包应用

```bash
npm run tauri build
```

## 核心命令速查

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run tauri dev` | 开发模式运行 |
| `npm run tauri build` | 构建生产版本 |
| `npm run dev` | 仅启动前端开发服务器 |
| `npm run build` | 仅构建前端 |

## 项目结构速览

```
easyPrinter/
├── src/                  # Vue 3 前端
│   ├── App.vue          # 主组件
│   └── components/      # 子组件
├── src-tauri/           # Rust 后端
│   └── src/main.rs      # 后端命令
├── package.json         # 依赖配置
└── README.md           # 详细文档
```

## 快速配置

### 修改服务器地址

编辑 `src-tauri/src/main.rs`：

```rust
let url = "https://your-server.com/config.json"; // 第 34 行
```

## 下一步

- 📖 查看 [README.md](./README.md) 了解完整功能
- 🔧 查看 [INSTALL.md](./INSTALL.md) 了解详细配置
- 🐛 遇到问题？查看故障排除部分

